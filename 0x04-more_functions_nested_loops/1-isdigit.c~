#include "main.h"
/**
 *_isdigit- check for digit
 *Return: 0
 */
int _isdigit(void)
{
int c;
if (c > 48 && c < 58)
{
_putchar(c + '1');
return (1);
}
if (c < 48 && c > 58)
{
_putchar(c + '0');
return (0);
}
}
